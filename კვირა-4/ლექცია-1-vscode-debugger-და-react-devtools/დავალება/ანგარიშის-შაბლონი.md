# ანგარიში: VS Code Debugger და React DevTools — Playlist Studio

**სტუდენტი:** ___________________
**თარიღი:** ___________________
**პროექტი:** `homework-react-devtools/`

---

## 1. პრობლემების ცხრილი

| # | სიმპტომი | ინსტრუმენტი | ფაილი : ხაზი | მიზეზი | გამოსწორება |
|---|---|---|---|---|---|
| 1 | | Components tab | | | |
| 2 | | State editing | | | |
| 3 | | Components + Console | | | |
| 4 | | VS Code Debugger | | | |
| 5 | | VS Code Debugger | | | |
| 6 | | Profiler | | | დიაგნოზი (გამოსწორება ლექცია 2) |

### კომპონენტთა ხე (Components tab-იდან)

```
App
├──
├──
└──
```

**TrackCard-ების რაოდენობა დასაწყისში:** _____
**App-ის state-ები:** ___________________

---

## 2. ბაგი #1 — პლეილისტის დუბლიკატები

**`playlist` props Components tab-იდან:**

| index | `id` | `title` | `count` |
|---|---|---|---|
| 0 | | | |
| 1 | | | |

**Console-ის გამაფრთხილებელი შეტყობინება:**

**VS Code VARIABLES:** `existing = ____`, `prevPlaylist.length = ____`, `track.id = ____`

**მიზეზი:**

**რატომ არ არის Console-ში წითელი შეცდომა:**

**გამოსწორება:**

```js
// before

// after
```

---

## 3. ბაგი #2 — ძიება

| `searchTerm` (DevTools-იდან) | ნაპოვნია |
|---|---|
| `Midnight` | |
| `midnight` | |
| `MIDNIGHT` | |
| `rain` | |
| `Rain` | |

**მიზეზი:**

**რა უპირატესობა აქვს state-ის DevTools-იდან შეცვლას:**

**გამოსწორება:**

```js
// before

// after
```

---

## 4. Highlight updates და render badge-ები

| კომპონენტი | renders (გახსნისას) | renders (5 ასოს შემდეგ) | გამართლებულია? |
|---|---|---|---|
| App | | | |
| FilterBar | | | |
| TrackList | | | |
| TrackCard | | | |
| PlaylistPanel | | | |
| StatsFooter | | | |

**კალათაში დამატებაზე ზედმეტად მოციმციმე კომპონენტები:**

**რატომ ხდება ეს (React-ის default ქცევა):**

---

## 5. ბაგი #3 — „დეტალები" არასწორ ბარათზე

| | სანამ 🚫 დააჭერდით | მას შემდეგ |
|---|---|---|
| დეტალები გახსნილია ტრეკზე | Paper Boats | |
| Console-ში unmount დაილოგა | — | |

**TrackCard-ის hooks Components tab-ში:** `State: ____`

**მიზეზი:**

**იგივე ბაგის მეორე სიმპტომი (ძიებით):**

**როდის არის `key={index}` უსაფრთხო:**

**გამოსწორება:**

```jsx
// before

// after
```

---

## 6. ბაგი #4 — ტრეკის ხანგრძლივობა

**გამოყენებული launch.json კონფიგურაცია:** ___________________

**WATCH პანელი:**

| ტრეკი | `totalSeconds` | `Math.round(/60)` | `Math.floor(/60)` | `% 60` | ეკრანზე | უნდა იყოს |
|---|---|---|---|---|---|---|
| Midnight Drive | | | | | | 3:45 |
| First Frost | | | | | | 2:09 |
| Paper Boats | | | | | | 3:18 |

**ორი დეფექტი ერთ ფუნქციაში:**

1.
2.

**რატომ არის „Paper Boats" შემთხვევით სწორი:**

**გამოსწორება:**

```js
// before

// after
```

**შემოწმება:** Midnight Drive `____`, Neon Rain `____`, Solar Wind `____`,
Night Bus `____`, Echo Chamber `____`, First Frost `____`, საშუალო `____`

---

## 7. ბაგი #5 — პლეილისტის ჯამური დრო

**WATCH expressions:**

| გამოსახულება | მნიშვნელობა |
|---|---|
| `playlist.length` | |
| `playlist[0].count` | |
| `playlist[0].duration` | |
| `playlist.reduce((s, i) => s + i.duration * i.count, 0)` | |

**მიზეზი:**

**რატომ არის `totalPlaylistTracks` სწორი, `totalPlaylistSeconds` კი — არა:**

**გამოსწორება:**

```js
// before

// after
```

**შემოწმება:** `Midnight Drive x3 + Neon Rain x1` → სულ ____ ტრეკი, ____ (მოსალოდნელი 15:20)

### VS Code vs Chrome DevTools

| | Chrome DevTools | VS Code |
|---|---|---|
| ცვლადების პანელი | | |
| Watch | | |
| Console/Evaluation | | |
| Step Over | | |

---

## 8. პრობლემა #6 — ნელი აკრეფა (Profiler)

**Commit-ების რაოდენობა:** _____
**ერთი commit-ის ხანგრძლივობა:** _____ ms

**Ranked view — ტოპ 4:**

| # | კომპონენტი | დრო (ms) |
|---|---|---|
| 1 | | |
| 2 | | |
| 3 | | |
| 4 | | |

**მძიმე გამოთვლა ფაილში:** ___________________

**Why did this render?**

| კომპონენტი | მიზეზი | გამართლებულია? |
|---|---|---|
| FilterBar | | |
| TrackList | | |
| TrackCard | | |
| PlaylistPanel | | |
| StatsFooter | | |

**StatsFooter:** `Props changed: (________)` — ეს prop იქმნება `App.jsx`-ის ხაზზე `____`

**რატომ არის ის „ახალი" ყოველ რენდერზე:**

**დიაგნოზი — რა უნდა შეიცვალოს (3-4 წინადადება):**

---

## 9. დამატებითი სავარჯიშოები

**9.1** Elements → track-card-ის შესაბამისი კომპონენტი: `____` | DOM-ზე გადასვლის ღილაკი: `____`

**9.2** Copy props-ის შედეგი და სად გამოგადგებოდათ:

**9.3** Rendered by ჯაჭვი: `____ → ____ → ____`

| hook (DevTools-ში) | კოდში |
|---|---|
| | |
| | |

**9.4** Debug Console:

```
playlist.map(i => i.title + ' x' + i.count)  →
typeof playlist[0].count                     →
```

**რითი განსხვავდება `console.log`-ის დამატებისგან:**

---

## 10. ბონუსი — ოპტიმიზაცია

| კომპონენტი | renders (before) | renders (after) |
|---|---|---|
| TrackCard | | |
| PlaylistPanel | | |
| StatsFooter | | |

**Commit-ის დრო:** before ____ ms → after ____ ms

**რატომ არ ეხმარებოდა `React.memo` StatsFooter-ს სანამ `settings` prop-ს არ გამოასწორეთ:**

**ყველაზე დიდი ეფექტის მომტანი ცვლილება:**

---

## 11. ვერიფიკაცია

- [ ] „Midnight Drive" ორჯერ → ერთი მწკრივი `x2`
- [ ] Console-ში `key`-ს გამაფრთხილებელი არ არის
- [ ] ძიება მუშაობს პატარა და დიდი ასოებით
- [ ] 🚫-ის შემდეგ დეტალები იმავე ტრეკზე რჩება
- [ ] Console-ში წაშლილი ტრეკის unmount ილოგება
- [ ] Midnight Drive 3:45, First Frost 2:09, Solar Wind 5:42
- [ ] საშუალო ხანგრძლივობა 3:55
- [ ] `x3 + x1` → 4 ტრეკი, 15:20
- [ ] Console სუფთაა
- [ ] `npm run build` წარმატებით სრულდება

---

## 12. დასკვნები

1. **რომელი ინსტრუმენტი აღმოჩნდა ყველაზე სასარგებლო და რატომ?**

2. **რომელი ბაგი ვერ იპოვებოდა Chrome-ის ჩვეულებრივი DevTools-ით (React DevTools-ის გარეშე) და რატომ?**

3. **როდის ჯობია VS Code Debugger Chrome DevTools-ს და პირიქით?**

4. **რას გაჩვენებთ Profiler, რასაც render badge-ები ვერ აჩვენებენ?**

---

## 13. Screenshot-ები

| # | აღწერა | ფაილის სახელი |
|---|---|---|
| 1 | Components → PlaylistPanel props დუბლიკატებით | |
| 2 | Components → searchTerm-ის ხელით შეცვლა + შედეგი | |
| 3 | TrackCard-ის hooks არასწორ ტრეკზე + Console unmount | |
| 4 | VS Code WATCH — Math.round / Math.floor / % 60 | |
| 5 | VS Code — playlist[0].count `totalPlaylistSeconds`-ში | |
| 6 | Profiler Ranked + StatsFooter „Why did this render?" | |
| 7 | ბონუსი — before/after render badge-ები | |
